package com.hybrid.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
    // Reserved for future rate limiting, CORS, or custom filters
}
